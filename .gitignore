node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*
# Tracked on purpose: it is the template `cp .env.example .env` copies from, and
# it holds only local Docker defaults. Ignoring it would break the quickstart.
!.env.example
.DS_Store
prisma/generated/

# Mintlify CLI local build output (apps/docs-site)
.mintlify/
.mint/

# Private working notes, not published
explanations.md
