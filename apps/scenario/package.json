{
  "name": "@arc/scenario",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "start": "node --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "@arc/chain": "workspace:*",
    "@arc/db": "workspace:*",
    "@arc/ledger": "workspace:*",
    "@arc/money": "workspace:*",
    "@arc/movement": "workspace:*"
  }
}
